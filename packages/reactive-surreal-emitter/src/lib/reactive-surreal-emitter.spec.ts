import { reactiveSurrealEmitter } from './reactive-surreal-emitter';

describe('reactiveSurrealEmitter', () => {
  it('should work', () => {
    expect(reactiveSurrealEmitter()).toEqual('reactive-surreal-emitter');
  });
});
