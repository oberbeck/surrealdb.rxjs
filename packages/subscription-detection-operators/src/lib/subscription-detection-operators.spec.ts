import { subscriptionDetectionOperators } from './subscription-detection-operators';

describe('subscriptionDetectionOperators', () => {
  it('should work', () => {
    expect(subscriptionDetectionOperators()).toEqual(
      'subscription-detection-operators'
    );
  });
});
